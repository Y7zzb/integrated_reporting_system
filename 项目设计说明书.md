# 问卷数据一体化处理系统设计说明书

---

## 文档信息

| 项目名称 | 问卷数据一体化处理系统 |
|---------|----------------------|
| 课程名称 | 抽样技术 |
| 团队人数 | 5人 |
| 编写日期 | 2026年6月 |
| 版本号 | V2.0 |

---

## 目录

1. 第一章 项目概述
   - 1.1 项目背景
   - 1.2 研究意义
   - 1.3 整体创新点
   - 1.4 系统目标

2. 第二章 系统架构设计
   - 2.1 整体架构
   - 2.2 模块划分
   - 2.3 数据流设计
   - 2.4 技术选型

3. 第三章 模块详细设计
   - 3.1 组员1：数据接入与标准化模块
   - 3.2 组员2：无效问卷筛查模块
   - 3.3 组员3：样本量计算模块
   - 3.4 组员4：量表信度分析模块
   - 3.5 组员5：综合报告生成模块

4. 第四章 系统集成与测试
   - 4.1 模块集成方案
   - 4.2 功能测试结果
   - 4.3 兼容性验证

5. 第五章 总结与展望
   - 5.1 系统整体总结
   - 5.2 问题反思
   - 5.3 后续优化方案

---

## 第一章 项目概述

### 1.1 项目背景

#### 1.1.1 现实需求

在抽样技术课程实践和社会调查研究中，问卷数据处理是一项耗时且易出错的工作。传统处理方式存在以下问题：

**（1）数据处理效率低下**

问卷星等平台导出的原始数据往往包含大量元信息（答卷编号、提交时间、IP地址等），需要人工筛选题目列进行后续分析。一份包含50道题目、500份问卷的数据，仅数据清洗就可能耗费2-3小时。

**（2）无效问卷识别主观性强**

传统无效问卷筛选主要依赖人工判断，缺乏统一标准。研究者可能因"作答时间过短"、"选项过于集中"等主观印象剔除问卷，但缺乏量化依据，导致筛选结果不可复现。

**（3）统计分析工具分散**

样本量计算、信度检验、描述性统计、图表生成等功能分散在不同软件中（SPSS、Excel、R等），研究者需要在多个工具间切换，数据格式转换繁琐。

**（4）报告撰写重复劳动**

每道题目的统计描述、图表绘制、分析文字撰写存在大量重复工作，且不同研究者的报告格式、表述风格差异较大。

#### 1.1.2 课程教学需求

抽样技术课程要求学生掌握：

- 样本量计算原理与公式推导
- 非抽样误差的识别与控制
- 无回答误差的评估方法
- 数据质量控制技术

本项目将理论知识转化为可操作的系统工具，实现"学以致用"的教学目标。

### 1.2 研究意义

#### 1.2.1 理论意义

**（1）非抽样误差量化研究**

系统将非抽样误差细分为四类：
- 覆盖误差（未覆盖目标总体）
- 抽样误差（随机抽样固有误差）
- 无回答误差（样本缺失导致）
- 计量误差（测量工具或回答偏差）

通过量化各类误差贡献率，为抽样理论提供实证支撑。

**（2）数据质量控制框架**

建立"数据接入→标准化→无效筛查→清洗→分析"的完整质量控制流程，形成可复用的数据处理范式。

**（3）样本量计算模型**

实现无限总体公式与有限总体校正的自动切换，考虑无效问卷缓冲，为抽样设计提供决策支持。

#### 1.2.2 实践意义

**（1）提升数据处理效率**

系统可在5分钟内完成500份问卷的完整处理流程，效率提升约20倍。

**（2）标准化研究流程**

统一的数据处理标准和报告格式，提高研究结果的可比性和可复现性。

**（3）降低技术门槛**

Web界面设计使非编程背景的研究者也能使用专业统计功能。

**（4）教学辅助工具**

为抽样技术课程提供实践平台，学生可通过操作理解抽象的统计概念。

### 1.3 整体创新点

#### 创新点一：智能题型识别

系统自动识别单选题、多选题、量表题三种题型：

| 识别规则 | 单选题 | 多选题 | 量表题 |
|---------|--------|--------|--------|
| 数据特征 | 选项数≤8，无分隔符 | 含逗号/分号分隔 | 数值范围1-5或1-7 |
| 处理方式 | 频数统计+饼图 | 选项拆分统计 | 均值+信度分析 |

#### 创新点二：多维无效问卷筛查

建立四维筛查指标体系：

```
无效问卷判定 = f(作答时长, 缺答率, 答案一致性, 内容有效性)
```

- **时间维度**：作答时长 < 题目数×2秒 或 < 45秒
- **完整维度**：缺答率 ≥ 40%
- **一致性维度**：统一答案 或 同一选项占比≥90%且熵≤0.75
- **内容维度**：无意义乱填占比 ≥ 60%

#### 创新点三：自适应样本量计算

```python
# 无限总体公式
n₀ = (Z² × p × (1-p)) / e²

# 有限总体校正（当 n₀ > 5%N 时启用）
n = n₀ / (1 + (n₀-1)/N)

# 实际建议发放量（含30%无效缓冲）
建议量 = ⌈n × 1.3⌉
```

系统自动判断是否需要有限总体校正，并给出达标判断。

#### 创新点四：批量图表+标准化文案

- **图表自适应**：选项过多自动合并"其他"，标签过小自动隐藏
- **文案自动生成**：根据统计结果生成符合学术规范的分析文字
- **多格式导出**：Markdown/JSON/CSV/Excel四种格式一键导出

#### 创新点五：AI辅助分析（可选）

支持DeepSeek、通义千问、豆包等大模型接口，将统计量转化为文字分析报告，实现"人机协作"的研究模式。

### 1.4 系统目标

#### 主要目标

1. **数据处理自动化**：实现从原始数据到分析报告的一键处理
2. **质量控制标准化**：建立可量化、可复现的无效问卷筛选标准
3. **统计功能集成化**：整合样本量计算、信度检验、描述性统计等核心功能
4. **报告生成规范化**：自动生成符合学术规范的统计报告

#### 性能指标

| 指标 | 目标值 |
|------|--------|
| 数据处理速度 | ≥100份/分钟 |
| 无效识别准确率 | ≥85% |
| 图表生成成功率 | ≥95% |
| 系统响应时间 | ≤3秒 |

---

## 第二章 系统架构设计

### 2.1 整体架构

系统采用分层架构设计：

```
┌─────────────────────────────────────────────────────────────┐
│                    表现层 (Presentation)                     │
│         Streamlit Web界面 / 命令行界面 / API接口              │
├─────────────────────────────────────────────────────────────┤
│                    业务层 (Business)                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │样本量计算│ │无效筛查  │ │量表分析  │ │报告生成  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│                    数据层 (Data)                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │数据接入  │ │标准化    │ │字段识别  │ │结果导出  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│                    基础层 (Infrastructure)                   │
│         Pandas / NumPy / Matplotlib / Requests               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 模块划分

| 模块编号 | 模块名称 | 负责组员 | 核心功能 |
|---------|---------|---------|---------|
| M1 | 数据接入与标准化 | 组员1 | 文件读取、格式转换、字段标准化 |
| M2 | 无效问卷筛查 | 组员2 | 四维筛查、误差统计、日志记录 |
| M3 | 样本量计算 | 组员3 | 公式计算、有限总体校正、达标判断 |
| M4 | 量表信度分析 | 组员4 | 克隆巴赫α、维度统计、分组对比 |
| M5 | 综合报告生成 | 组员5 | 图表生成、文案撰写、多格式导出 |

### 2.3 数据流设计

```
┌─────────────┐
│  原始数据   │  Excel/CSV
│  (Raw Data) │
└──────┬──────┘
       │ M1: 数据接入
       ↓
┌─────────────┐
│  标准化数据 │  统一列名、空值处理
│ (Standard)  │
└──────┬──────┘
       │ M2: 无效筛查
       ↓
┌─────────────┐
│  有效样本   │  剔除无效问卷
│  (Valid)    │
└──────┬──────┘
       │ M3/M4: 统计分析
       ↓
┌─────────────┐
│  统计结果   │  频数、均值、信度
│ (Statistics)│
└──────┬──────┘
       │ M5: 报告生成
       ↓
┌─────────────┐
│  分析报告   │  图表+文字+数据
│  (Report)   │
└─────────────┘
```

### 2.4 技术选型

| 技术层 | 技术选型 | 选型理由 |
|-------|---------|---------|
| 开发语言 | Python 3.10+ | 丰富的数据分析库生态 |
| 数据处理 | Pandas, NumPy | 高效的数据结构和计算能力 |
| 可视化 | Matplotlib | 灵活的图表定制能力 |
| Web框架 | Streamlit | 快速构建数据应用界面 |
| Excel处理 | openpyxl | 读写Excel文件 |
| AI接口 | Requests | HTTP请求调用大模型API |

---

## 第三章 模块详细设计

### 3.1 组员1：数据接入与标准化模块

#### 3.1.1 功能概述

负责问卷数据的读取、格式转换和标准化处理，为后续分析提供统一的数据结构。

#### 3.1.2 核心功能

**（1）多格式数据读取**

```python
def load_data(file_path: str | Path) -> Tuple[pd.DataFrame, Dict[str, str]]:
    """
    支持：.csv, .xlsx, .xls, .xlsm
    CSV编码自动识别：utf-8-sig, utf-8, gbk, gb18030
    """
```

**（2）列名标准化**

- 去除空白字符
- 处理重复列名（自动添加后缀）
- 处理未命名列（自动编号）

**（3）字段分类识别**

```python
META_KEYWORDS = (
    "答卷编号", "提交时间", "所用时间", "来源", "IP",
    "省份", "城市", "设备", "浏览器", "昵称", "姓名", ...
)

def infer_columns(data: pd.DataFrame) -> Dict[str, List[str]]:
    """
    返回：{
        "metadata_columns": [...],  # 元信息列
        "answer_columns": [...]     # 题目列
    }
    """
```

**（4）数据质量概览**

生成DataProfile数据类，包含：
- 文件基本信息
- 行列数量
- 缺失情况
- 重复行数
- 质量警告

#### 3.1.3 输出文件

| 文件名 | 内容 |
|-------|------|
| raw_data_snapshot.csv | 原始数据快照 |
| standard_data.csv | 标准化后数据 |
| data_profile.csv | 数据质量概览 |
| column_info.json | 字段分类结果 |

### 3.2 组员2：无效问卷筛查模块

#### 3.2.1 功能概述

基于多维指标对问卷进行质量筛查，识别并剔除无效答卷，计算各类误差率。

#### 3.2.2 筛查指标体系

**指标一：作答时长**

```python
# 动态阈值计算
duration_threshold = max(
    min_duration_seconds,           # 最小45秒
    answer_count × seconds_per_answer  # 题目数×2秒
)

# 或基于数据分布自适应
duration_threshold = max(base, q10×0.8, q25×0.6)
```

**指标二：缺答率**

```python
missing_ratio = missing_count / total_questions
if missing_ratio >= 0.40:  # 缺答超过40%
    reasons.append("大面积缺答")
```

**指标三：答案一致性**

```python
# 统一答案检测
if unique_answers == 1:
    reasons.append("统一答案")

# 疑似直线作答检测
same_ratio = max(freq) / total
entropy = -Σ(p × log₂(p))
if same_ratio >= 0.90 and entropy <= 0.75:
    reasons.append("疑似直线作答")
```

**指标四：内容有效性**

```python
def _looks_like_nonsense(text: str) -> bool:
    """
    识别无意义内容：
    - 纯重复字符：aaaa, 1111
    - 纯字母数字混合：abc123
    - 无实际语义符号
    """
```

#### 3.2.3 误差统计

```python
@dataclass
class ScreeningResult:
    total_rows: int           # 总问卷数
    valid_rows: int           # 有效问卷数
    invalid_rows: int         # 无效问卷数
    valid_rate: float         # 有效率
    invalid_rate: float       # 无效率
    non_sampling_error_rate: float    # 非抽样误差率
    nonresponse_error_rate: float     # 无回答误差率
    reason_breakdown: Dict[str, int]  # 剔除原因分布
```

#### 3.2.4 输出文件

| 文件名 | 内容 |
|-------|------|
| cleaned_data.csv | 有效问卷数据 |
| invalid_data.csv | 无效问卷数据 |
| filter_log.csv | 逐条筛查日志 |
| error_report.csv | 误差率汇总 |
| reason_statistics.csv | 剔除原因统计 |

### 3.3 组员3：样本量计算模块

#### 3.3.1 功能概述

根据置信度、允许误差、总体规模等参数，计算理论最小样本量，并判断实际样本是否达标。

#### 3.3.2 计算公式

**（1）无限总体样本量公式**

$$n_0 = \frac{Z^2 \times p \times (1-p)}{e^2}$$

其中：
- $Z$：置信水平对应的Z值（95%→1.96，99%→2.576）
- $p$：预期比例（最保守取0.5）
- $e$：允许误差

**（2）有限总体校正**

当 $n_0 > 5\%N$ 时启用：

$$n = \frac{n_0}{1 + \frac{n_0 - 1}{N}}$$

**（3）建议发放量**

考虑约30%无效问卷缓冲：

$$建议量 = \lceil n \times 1.3 \rceil$$

#### 3.3.3 核心函数

```python
def calculate_sample(
    confidence: float = 0.95,      # 置信度
    margin_error: float = 0.05,   # 允许误差
    p: float = 0.5,               # 预期比例
    population: int = 10000,      # 总体数量
    actual_sample: int = 0,       # 实际样本数
) -> dict:
    """
    返回：
    - min_sample: 理论最小样本量
    - suggested: 建议发放量
    - is_sufficient: 是否达标
    - gap: 差距（若不足）
    """
```

#### 3.3.4 置信度与Z值对照

| 置信度 | Z值 |
|-------|-----|
| 90% | 1.645 |
| 95% | 1.96 |
| 99% | 2.576 |

### 3.4 组员4：量表信度分析模块

#### 3.4.1 功能概述

针对李克特量表数据，计算克隆巴赫α信度系数，进行维度统计和分组对比分析。

#### 3.4.2 克隆巴赫α系数

**计算公式**：

$$\alpha = \frac{k \times \bar{r}}{1 + (k-1) \times \bar{r}}$$

其中：
- $k$：题项数量
- $\bar{r}$：题项间平均相关系数

**信度评价标准**：

| α值范围 | 评价 |
|--------|------|
| α ≥ 0.9 | 信度极佳 |
| 0.8 ≤ α < 0.9 | 信度良好 |
| 0.7 ≤ α < 0.8 | 信度可接受 |
| α < 0.7 | 信度偏低 |

#### 3.4.3 核心功能

**（1）题项统计**

```python
item_statistics = {
    "题项编号": "Q1",
    "平均分": 3.85,
    "标准差": 0.92,
    "方差": 0.85,
    "删除本题后α系数": 0.82  # 用于识别可删除题项
}
```

**（2）维度分析**

支持用户自定义维度分组：
```
维度名称=Q1,Q2,Q3
服务质量=Q4,Q5,Q6
```

**（3）反向题处理**

- 自动识别：均值最低的20%题项
- 手动补充：用户指定反向题
- 自动反转：6 - 原始得分（针对5点量表）

**（4）分组对比**

按性别、组别等分类变量进行均值对比和可视化。

#### 3.4.4 输出内容

| 输出项 | 内容 |
|-------|------|
| 题项统计表 | 各题均值、标准差、删除后α |
| 整体信度 | 克隆巴赫α系数及评价 |
| 维度统计 | 各维度均值、标准差、信度 |
| 分组对比 | 各组在各题/维度的均值差异 |

### 3.5 组员5：综合报告生成模块

#### 3.5.1 功能概述

整合所有模块输出，批量生成图表，自动撰写分析文案，导出多格式报告。

#### 3.5.2 图表生成系统

**（1）单选题图表**

| 图表类型 | 说明 |
|---------|------|
| 饼图 | 展示选项占比，环形设计 |
| 柱状图 | 展示频数分布，支持横向/纵向 |

**（2）多选题图表**

- 水平柱状图
- 显示各选项选择次数和占比

**（3）量表题图表**

- 直方图：展示得分分布
- 均值标记线
- 自动适配5点/7点量表

**（4）图表样式自适应**

```python
class ChartConfig:
    width: int = 10           # 图表宽度
    height: int = 6           # 图表高度
    dpi: int = 150            # 分辨率
    max_categories: int = 10  # 最大显示类别数
    color_scheme: str         # 配色方案
```

- 选项过多自动合并"其他"
- 标签过小自动隐藏
- 中文字体自动适配

#### 3.5.3 标准化分析文案

**单选题文案模板**：
```
本题共收到N份有效回答，缺失M份（缺失率X%）。
从频数分布来看，[选项A]选择人数最多，占比Y%，
表明该选项具有明显集中趋势/分布较为均衡。
均值=X.XX，标准差=X.XX，说明回答离散程度较低/中等/较高。
```

**量表题文案模板**：
```
本题为量表题，有效回答N份，缺失M份（缺失率X%）。
均值=X.XXX，中位数=X.XXX，标准差=X.XXX。
整体偏向正向（同意）/中立/负向（不同意），
数据离散程度较低/中等/较高。
```

#### 3.5.4 多格式报告导出

| 格式 | 文件 | 内容 | 适用场景 |
|------|------|------|---------|
| Markdown | .md | 完整报告 | 课程论文 |
| JSON | .json | 结构化数据 | 程序调用 |
| CSV | .csv | 统计汇总表 | Excel分析 |
| Excel | .xlsx | 多Sheet报告 | 正式报告 |

**Excel报告结构**：
- Sheet1：报告摘要
- Sheet2：单选题统计
- Sheet3：多选题统计
- Sheet4：量表维度统计
- Sheet5：全部统计汇总

#### 3.5.5 AI辅助分析（可选）

支持调用大模型API生成文字分析报告：

```python
def generate_ai_analysis(
    report: Dict,
    api_key: str,
    base_url: str,
    model: str,
    user_requirement: str = "",
) -> Tuple[str, dict]:
    """
    支持服务商：
    - DeepSeek
    - 通义千问（阿里百炼）
    - 豆包（火山方舟）
    - 自定义OpenAI兼容接口
    """
```

---

## 第四章 系统集成与测试

### 4.1 模块集成方案

#### 4.1.1 统一入口

**命令行模式**：
```bash
python integrated_reporting_system.py <问卷文件> -o <输出目录>
```

**Web界面模式**：
```bash
streamlit run integrated_platform.py
```

#### 4.1.2 模块依赖关系

```
integrated_platform.py (统一界面)
    │
    ├── questionnaire_platform_core.py (核心功能)
    │       ├── process_questionnaire()
    │       ├── calculate_sample()
    │       └── generate_ai_analysis()
    │
    ├── integrated_reporting_system.py (报告生成)
    │       ├── ComprehensiveAnalyzer
    │       ├── ChartGenerator
    │       └── ReportExporter
    │
    └── group2_invalid_questionnaire_screening1.py (数据处理)
            ├── load_data()
            ├── normalize_dataframe()
            ├── screen_questionnaires()
            └── clean_valid_data()
```

### 4.2 功能测试结果

#### 4.2.1 测试环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 10/11 |
| Python版本 | 3.10+ |
| 测试数据 | 模拟问卷数据（100份） |

#### 4.2.2 测试用例与结果

| 测试项 | 测试内容 | 预期结果 | 实际结果 |
|-------|---------|---------|---------|
| T1 | 模块导入 | 全部成功 | ✅ 通过 |
| T2 | 图表生成 | 饼图/柱状图/直方图 | ✅ 通过 |
| T3 | 数据处理 | 标准化+筛查 | ✅ 通过 |
| T4 | 综合分析 | 批量分析+报告导出 | ✅ 通过 |
| T5 | 样本计算 | 公式计算+校正 | ✅ 通过 |

#### 4.2.3 性能测试

| 数据规模 | 处理时间 | 内存占用 |
|---------|---------|---------|
| 100份问卷 | <5秒 | <200MB |
| 500份问卷 | <30秒 | <500MB |
| 1000份问卷 | <60秒 | <800MB |

### 4.3 兼容性验证

#### 4.3.1 已解决的兼容性问题

| 问题 | 解决方案 |
|------|---------|
| CSV编码不一致 | 自动尝试utf-8-sig/utf-8/gbk/gb18030 |
| 中文字体缺失 | 自动检测系统可用中文字体 |
| Excel列名重复 | 自动添加数字后缀 |
| 空值表示不统一 | 统一处理NaN/None/空字符串/NULL |

#### 4.3.2 支持的输入格式

| 格式 | 扩展名 | 编码 |
|------|-------|------|
| Excel | .xlsx, .xls, .xlsm | 自动 |
| CSV | .csv | utf-8-sig/utf-8/gbk |

---

## 第五章 总结与展望

### 5.1 系统整体总结

#### 5.1.1 完成的核心功能

**✅ 数据处理全流程自动化**

从原始问卷数据到分析报告的一键处理，实现了：
- 数据接入与标准化
- 无效问卷智能筛查
- 统计分析自动化
- 图表批量生成
- 报告多格式导出

**✅ 抽样理论实践结合**

将课程理论知识转化为可操作工具：
- 样本量计算公式实现
- 非抽样误差量化统计
- 无回答误差评估
- 数据质量控制流程

**✅ 用户友好的交互设计**

- Web界面直观易用
- 参数配置灵活
- 实时结果预览
- 详细使用说明

#### 5.1.2 系统特色亮点

| 特色 | 说明 |
|------|------|
| 智能识别 | 自动识别题型、反向题、元信息列 |
| 自适应设计 | 图表样式、阈值参数自动调整 |
| 标准化输出 | 统一的报告格式和分析文案 |
| 模块化架构 | 各模块独立可复用，便于扩展 |
| 可扩展性 | 支持自定义配置、AI接口扩展 |

#### 5.1.3 项目成果统计

| 指标 | 数量 |
|------|------|
| 代码文件 | 9个 |
| 代码总行数 | 约3500行 |
| 核心函数 | 50+ |
| 支持图表类型 | 6种 |
| 报告导出格式 | 4种 |

### 5.2 问题反思

#### 5.2.1 开发过程中的问题

**（1）模块接口协调**

初期各模块独立开发，接口定义不统一，集成时出现数据格式不匹配问题。

**解决**：统一使用Pandas DataFrame作为数据交换格式，定义明确的数据类（DataClass）规范接口。

**（2）中文字体问题**

Matplotlib默认不支持中文，图表显示乱码。

**解决**：实现字体自动检测机制，优先使用SimHei、Microsoft YaHei等常见中文字体。

**（3）异常处理不完善**

部分边界情况未考虑，如空数据、全缺失列等导致程序崩溃。

**解决**：增加全面的输入验证和异常捕获，提供友好的错误提示。

#### 5.2.2 现有局限性

| 局限 | 说明 |
|------|------|
| 题型识别局限 | 仅支持单选/多选/量表三种，不支持填空题智能分析 |
| 筛查规则固定 | 阈值参数可调，但筛查逻辑固定，不支持自定义规则 |
| 图表定制有限 | 图表样式预设，不支持用户深度定制 |
| 无效识别准确率 | 对"认真乱填"等复杂情况识别率有限 |

#### 5.2.3 经验教训

1. **前期设计重要性**：应在开发前充分讨论接口规范，避免后期大量重构
2. **测试驱动开发**：应先编写测试用例，再实现功能，确保代码质量
3. **文档同步更新**：代码修改后应及时更新文档，避免文档与代码不一致
4. **用户体验优先**：技术实现应服务于用户体验，而非炫技

### 5.3 后续优化方案

#### 5.3.1 短期优化（1-2周）

**优化一：增强题型识别**

```python
# 新增题型识别
- 填空题：文本长度分布分析
- 矩阵题：多行多列结构识别
- 排序题：数值序列检测
```

**优化二：自定义筛查规则**

```python
class CustomScreeningRule:
    name: str              # 规则名称
    condition: Callable    # 判断函数
    weight: float          # 权重（用于综合评分）
    
# 支持用户自定义筛查规则并动态加载
```

**优化三：图表交互增强**

- 支持图表缩放、平移
- 鼠标悬停显示详细数据
- 图表导出为SVG/PDF矢量格式

#### 5.3.2 中期优化（1-2月）

**优化一：机器学习辅助筛查**

```python
# 使用历史标注数据训练分类模型
from sklearn.ensemble import RandomForestClassifier

model.fit(features, labels)  # features: 答题特征
                           # labels: 有效/无效标注
```

提升对复杂无效问卷的识别准确率。

**优化二：多语言支持**

- 英文界面选项
- 分析文案多语言版本
- 国际化字体适配

**优化三：协作功能**

- 多用户项目管理
- 数据版本控制
- 处理流程共享

#### 5.3.3 长期规划（3-6月）

**规划一：Web平台升级**

- 从Streamlit迁移到Django/Flask
- 用户认证与权限管理
- 数据库存储（PostgreSQL）
- RESTful API接口

**规划二：移动端支持**

- 响应式Web设计
- 移动端数据采集App
- 实时数据同步

**规划三：智能分析增强**

- 自动生成研究结论
- 异常数据预警
- 抽样方案优化建议

#### 5.3.4 优化优先级矩阵

| 优化项 | 重要性 | 紧急性 | 优先级 |
|-------|-------|-------|-------|
| 增强题型识别 | 高 | 高 | P1 |
| 自定义筛查规则 | 高 | 中 | P1 |
| 图表交互增强 | 中 | 高 | P2 |
| 机器学习辅助 | 高 | 低 | P2 |
| 多语言支持 | 低 | 低 | P3 |
| Web平台升级 | 中 | 低 | P3 |

---

## 附录

### 附录A：文件清单

| 文件名 | 功能 | 代码行数 |
|-------|------|---------|
| ai_client.py | AI接口客户端 | 150 |
| group2_invalid_questionnaire_screening.py | 无效筛查v1 | 500 |
| group2_invalid_questionnaire_screening1.py | 无效筛查v2 | 900 |
| questionnaire_platform_core.py | 平台核心功能 | 280 |
| questionnaire_streamlit_app.py | 原Web界面 | 200 |
| 量表独立工具.py | 量表分析工具 | 195 |
| integrated_reporting_system.py | 综合报告系统 | 1250 |
| integrated_platform.py | 整合平台界面 | 510 |
| run_tests.py | 系统测试脚本 | 315 |

### 附录B：依赖库清单

```
pandas>=1.5.0
numpy>=1.21.0
matplotlib>=3.5.0
openpyxl>=3.0.0
streamlit>=1.20.0
requests>=2.28.0
Pillow>=9.0.0
reportlab>=3.6.0
python-docx>=0.8.11
scipy>=1.7.0
```

### 附录C：使用示例

**示例1：命令行完整处理**

```bash
python integrated_reporting_system.py questionnaire.xlsx -o output --chart-dpi 200 --color-scheme professional
```

**示例2：Web界面使用**

```bash
streamlit run integrated_platform.py
```

访问 http://localhost:8501 进行交互操作。

**示例3：编程调用**

```python
from integrated_reporting_system import integrate_all_modules, ReportExporter
from group2_invalid_questionnaire_screening1 import process_file

# 处理问卷
result = process_file("questionnaire.xlsx", output_dir="output")

# 生成综合报告
report = integrate_all_modules(
    data=result["cleaned_data"],
    column_info=result["column_info"],
    screening_result=result["screening_result"],
    output_dir="output"
)

# 导出报告
exporter = ReportExporter(report, output_dir="output")
exporter.export_all_formats()
```

---

## 文档修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|---------|-------|
| V1.0 | 2026-05-31 | 初稿完成 | 全体组员 |
| V2.0 | 2026-06-02 | 整合所有模块，完善文档 | 全体组员 |

---

**文档结束**

*本设计说明书由抽样技术课程项目组共同编写，如有疑问请联系各模块负责人。*
